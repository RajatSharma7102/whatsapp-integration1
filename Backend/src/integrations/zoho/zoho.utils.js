// Utility functions for Zoho integration

const formatZohoPayload = (data) => {
    // Format data for Zoho API requests
    return data;
};

const parseZohoResponse = (response) => {
    // Parse response from Zoho API
    return response;
};

module.exports = {
    formatZohoPayload,
    parseZohoResponse
};
